/* Configurações de CSS (cores e fontes) devem ser feitas diretamente 
no arquivo src/tailwind.config.js seguindo a documentação do Tailwind CSS */

export const config = {
    //URI para consumo da API na pasta server
    apiURI: 'http://localhost:5000/',

    //Use apenas o nome do arquivo na pasta assets
    assets: {
        logo: 'logoipsum.svg'
    },
}